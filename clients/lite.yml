client: lite
displayName: "LPsoft Lite"
version: 0.1.0
delivery: binary

# features: LISTA de nomes contratados (mesma do _client-example.yml).
# lite = nenhuma → `[]` (equivale a copiar o exemplo e comentar TODAS).
features: []

ports:
  backend: 8132
  frontend: 3052
  db: 5484
